generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Cat {
  id    Int    @id @default(autoincrement())
  name  String
  age   Int
  breed String

  @@map("cats")
}
